# Copy to frontend/.env for local development. Never commit the filled file.
# Physical phones must use the development machine's reachable LAN IP, not localhost.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:8000/api
