import { Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { colors } from "@/constants/theme";

export function Field({ label, value, onChangeText, secureTextEntry, keyboardType = "default", placeholder }: { label: string; value: string; onChangeText: (value: string) => void; secureTextEntry?: boolean; keyboardType?: "default" | "email-address" | "numeric"; placeholder?: string }) { return <View style={styles.group}><Text style={styles.label}>{label}</Text><TextInput accessibilityLabel={label} value={value} onChangeText={onChangeText} secureTextEntry={secureTextEntry} keyboardType={keyboardType} autoCapitalize="none" placeholder={placeholder} style={styles.input} /></View>; }
export function Button({ title, onPress, disabled }: { title: string; onPress: () => void; disabled?: boolean }) { return <Pressable accessibilityRole="button" disabled={disabled} onPress={onPress} style={[styles.button, disabled && styles.disabled]}><Text style={styles.buttonText}>{title}</Text></Pressable>; }
export function ErrorText({ text }: { text?: string }) { return text ? <Text accessibilityLiveRegion="polite" style={styles.error}>{text}</Text> : null; }
const styles = StyleSheet.create({ group: { marginBottom: 14 }, label: { color: colors.ink, fontWeight: "600", marginBottom: 6 }, input: { borderColor: colors.border, borderWidth: 1, borderRadius: 12, padding: 13, fontSize: 16, color: colors.ink }, button: { backgroundColor: colors.purple, alignItems: "center", borderRadius: 14, padding: 16, marginTop: 8 }, disabled: { opacity: 0.55 }, buttonText: { color: colors.white, fontSize: 16, fontWeight: "700" }, error: { color: colors.danger, marginBottom: 12 } });
